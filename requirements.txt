# ONNX Runtime (CPU support for Docker)
onnxruntime>=1.20.1

# Web Framework
flask>=3.0.0
flask-cors>=6.0.0,<7.0.0
werkzeug>=3.0.0
gunicorn>=20.1.0

# HTTP Client
requests>=2.32.0

# Image Processing
Pillow>=12.0.0
opencv-python>=4.12.0.88

# NumPy (required by ONNX Runtime)
numpy>=1.24.0

# PyTorch (for TorchScript model support)
torch>=2.0.0

# Build tooling — setuptools 81.0.0 is the first release whose vendored build
# deps are patched: it bundles jaraco.context 6.1 (CVE-2026-23949) and
# wheel 0.46.3 (CVE-2026-24049) under setuptools/_vendor/, which scanners flag on
# older setuptools. Constrained to a range: the floor (81.0.0) stops any
# dependency pulling an older, vulnerable setuptools back in after the Dockerfile
# upgrade; the <84.0.0 cap guards reproducibility against a future major.
setuptools>=81.0.0,<84.0.0

# S3 access (Scaleway / AWS compatible)
boto3>=1.34.0
