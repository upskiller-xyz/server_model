# ONNX Runtime (CPU support for Docker)
onnxruntime>=1.20.1

# Web Framework
flask>=3.0.0
flask-cors>=6.0.0,<7.0.0
werkzeug>=3.0.0
gunicorn>=20.1.0

# HTTP Client
requests>=2.32.0

# Image Processing
Pillow>=11.4.0
opencv-python>=4.12.0.88

# NumPy (required by ONNX Runtime)
numpy>=1.24.0

# PyTorch (for TorchScript model support)
torch>=2.0.0

# Utilities

# S3 access (Scaleway / AWS compatible)
boto3>=1.34.0
